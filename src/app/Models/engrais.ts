

export interface Engrais {
   
        id: number;
        nom: string;
        image?: string; // L'image est optionnelle
        created_at?: string;
        updated_at?: string;
        variete?: string;
        quantite?: number;
   
}
